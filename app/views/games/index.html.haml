%h1 Listing games

%table
  %thead
    %tr
      %th Name
      %th Amount of work
      %th Complexity
      %th Unknown risk
      %th
      %th
      %th

  %tbody
    - @games.each do |game|
      %tr
        %td= game.name
        %td= game.amount_of_work
        %td= game.complexity
        %td= game.unknown_risk
        %td= link_to 'Show', game
        %td= link_to 'Edit', edit_game_path(game)
        %td= link_to 'Destroy', game, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Game', new_game_path
